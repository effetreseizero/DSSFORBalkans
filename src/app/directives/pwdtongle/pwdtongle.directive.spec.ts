import { PwdtongleDirective } from './pwdtongle.directive';

describe('PwdtongleDirective', () => {
  it('should create an instance', () => {
    const directive = new PwdtongleDirective();
    expect(directive).toBeTruthy();
  });
});
